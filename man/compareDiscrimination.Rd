% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotResults.R
\name{compareDiscrimination}
\alias{compareDiscrimination}
\title{Compare discrimination metrics across two analyses}
\usage{
compareDiscrimination(
  results,
  analysisIdA,
  analysisIdB,
  metrics = "AUROC",
  joinVars = c("targetId", "outcomeId", "legendLabel", "startDate", "endDate")
)
}
\arguments{
\item{results}{Tibble of model performance rows (e.g., `allResults`).}

\item{analysisIdA}{Identifier of the reference analysis (e.g., `"val_original"`).}

\item{analysisIdB}{Identifier of the comparison analysis (e.g., `"val_new_recalibrated"`).}

\item{metrics}{Character vector of metric columns to compare. Defaults to `"AUROC"`.}

\item{joinVars}{Character vector of columns used to align the rows between analyses.
Defaults to matching on `targetId`, `outcomeId`, `legendLabel`, `startDate`, and `endDate`.}
}
\value{
A tibble containing the matched rows with metric values from both analyses
  and a `_diff` column (analysis B minus analysis A) for each requested metric.
}
\description{
Compare discrimination metrics across two analyses
}
